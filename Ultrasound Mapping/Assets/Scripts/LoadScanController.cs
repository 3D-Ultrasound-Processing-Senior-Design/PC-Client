using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using System.IO;

//  public class Scan{
//         public string Name {get; set;}
//         public Angle = new float[3];
//         public Position = new float[3];

// }

public class LoadScanController : MonoBehaviour
{
    //List<Scan> scanList = new List<Scan>();
    //List<string> listB = new List<string>();
   
    //Scan tempScan;

    // //private LoadFileController path = gameObject.<GetComponent<LoadFileController>();
    // private static string path;
    // public void OpenFile(){
    //     path = LoadFileController.destinationPath;
    // using(var reader = new StreamReader(path))
    //     {
    //         while (!reader.EndOfStream)
    //         {
    //             var line = reader.ReadLine();
    //             var values = line.Split(',');
    //             values[0] = tempScan.Name; // add name category
    //             values[1] = tempScan.Angle[0]; // populate scan 
    //             values[2] = tempScan.Angle[1];
    //             values[3] = tempScan.Angle[2];

    //             values[4] = tempScan.Position[0];
    //             values[5] = tempScan.Position[1];
    //             values[6] = tempScan.Position[2];

    //             scanList.Add(tempScan);
    //         }
    //     }
        
    // }
}
